import streamlit as st

st.set_page_config(page_title="Химия 10", layout="wide")
st.title("🧪 Органикалық химия — зертхана")

# 19 сабақ
lessons = {
    i: f"{i}-сабақ" for i in range(1,20)
}

lesson = st.sidebar.selectbox("Сабақты таңда:", list(lessons.keys()), format_func=lambda x: lessons[x])

st.subheader(f"Сабақ {lesson}")

# ---- Интерактивті «пробирка» (қарапайым, бірақ сенімді) ----

st.write("🔬 Реакцияны бастау үшін батырманы бас:")

col1, col2, col3 = st.columns(3)

with col1:
    left = st.button("Сол пробирканы құю")

with col3:
    right = st.button("Оң пробирканы құю")

st.markdown("---")

if left and right:
    st.success("✅ Реакция жүрді!")
    st.balloons()

elif left:
    st.info("🟦 Сол пробирка құйылды → реакция күтіп тұр")
elif right:
    st.info("🟥 Оң пробирка құйылды → реакция күтіп тұр")
else:
    st.warning("Екі пробирканы да құйыңыз")

# ---- Сабаққа байланысты түс ----
colors = {
    1:"көк", 2:"жасыл", 3:"сары", 4:"көгілдір", 5:"қызғылт",
    6:"қызыл",7:"жасыл",8:"көк",9:"қоңыр",10:"алтын",
    11:"күлгін",12:"жасыл",13:"сұр",14:"сары",15:"көк",
    16:"қызыл",17:"жасыл",18:"көк",19:"қызыл"
}

st.write(f"Бұл сабақтағы реакция түсі: **{colors[lesson]}**")
